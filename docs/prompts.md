# Примеры промптов для Copilot Chat Agent

## Проверочные запросы (5 штук)

- Привет, Клодик! Помоги мне создать MCP сервер. Мне интересно понимать ширину рек и количество завалов на них по спутниковым снимкам. Как я могу это сделать? Помоги с выбором языков программирования, подходящими моделями и инструментами, а также с чего начать.

- В какой IDE будешь подключать MCP-сервер? От этого зависит формат конфига. Ответ: VS Code. Как работаем с данными? Ответ: локальные GeoTIFF в репозитории.

- Добавь конфиг для VS Code.

- Подскажи, откуда мне скачать файлы для проекта и как это запустить? Я имею в виду реальные данные рек.

- Подбери конкретный bbox под реку на Урале в Свердловской области с заметными меандрами или разветвлением русла, где разброс ширины и сужения будут видны нагляднее для демонстрации.

---

## Примеры ошибок и отладка

- Found python3: Python 3.14.5. Python process spawned, waiting for server startup... [STDERR] ModuleNotFoundError: No module named 'mcp'. MCP process exited with code 1.

- PowerShell error: .venv\Scripts\Activate.ps1 не может быть загружен, так как выполнение сценариев отключено в этой системе. CategoryInfo: ошибка безопасности.

- Не понимаю пункт про использование инструментов в Copilot Chat. Выбрала агент, выбрала режим локально, но инструменты не появились.

- Получилось! Доступна 1 сцена: synthetic_river.tif, размер 512 × 512, 4 канала, CRS EPSG:32633, разрешение 10 м/пиксель.

- Что дальше можно сделать? Как убедиться, что всё считается правильно?

---

## Реальные данные и анализ

- Загрузила файл tavda.tif. Что дальше делать с ней?

- Выполняю построение маски MNDWI для `tavda.tif`. Ошибка: `autodetected range of [nan, nan] is not finite`. Пиксельные значения оказались нечисловыми или сцена содержит `NaN`.

- По координатам находится сам город, а не река. Какую часть реки вы использовали?

- Рисунок реки обрезан по мосту через реку. С этим можно что-то сделать? Можно ли определить, что там мост и указать на это?

- В папке не появилась новая картинка, хотя ответ успешен. Может быть, файлы не записались на диск?

---

## Поиск завалов на реках

- Я нашла информацию про поиск завалов на реке. Методы: геометрический фильтр (резкое сужение или разрыв в маске воды) и текстурный анализ с OpenCV (фильтры Кэнни). Можем ли мы это применить в нашем проекте?

- Ладно, пока не делаем ничего с завалами. Сосредоточимся на основном функционале.

---

## Проверка по требованиям

- Работа считается выполненной, если: MCP-сервер запускается по инструкции, реализованы минимум 2–4 инструмента с описаниями, интеграция с IDE показана, есть минимум 5 запросов (минимум 3 вызывают tool), добавлены логи, секреты не закоммичены.

- Ошибка в README: написано 4 инструмента, но должно быть 5.

- Не понимаю блок про конфиг Claude Desktop. Куда именно в конфиг добавлять пути?

- Оставь в README только VS Code, уберу Claude Desktop.

- Помоги выполнить требование про вызов tool агентом: нужно 1 пример запроса, ожидаемый tool, подтверждение из логов/скриншота с указанием где это хранится в репо.

- Напиши список команд для создания скриншотов с рекой Тавда (файл tavda.tif уже в проекте).
