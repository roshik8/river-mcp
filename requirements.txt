# River satellite MCP server - runtime dependencies
# Install:  pip install -r requirements.txt   (Python >= 3.10, 3.12 recommended)
mcp[cli]>=1.27,<2
rasterio>=1.4,<2
numpy>=2.0
scipy>=1.13
scikit-image>=0.24
shapely>=2.0
matplotlib>=3.8        # optional: only used for PNG mask previews
